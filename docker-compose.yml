version: '3'
services:
  postgres:
    image: postgres
  tests:
    build: .
    depends_on:
      - postgres
    volumes:
      - .:/usr/src/app
    command: nimble test
  test:
    build: .
    depends_on:
      - postgres
    volumes:
      - .:/usr/src/app
    entrypoint: nim c -r
  docs:
    image: nimlang/nim
    volumes:
      - .:/usr/src/app
    command: "nim doc --project /usr/src/app/src/norm.nim"
